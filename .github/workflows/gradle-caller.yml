name: Call Environment Reusable Workflows

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    
permissions: read-all    

jobs:
  build-and-test:
    uses: ashu-parida-hpe/github_action_demo/.github/workflows/gradle.yml@main
